<%-- 登录状态检查 --%>
<%
    Object _user = session.getAttribute("user");
    if (_user == null) {
        response.sendRedirect(request.getContextPath() + "/login.jsp");
        return;
    }
    // 将用户信息存入request供页面使用
    request.setAttribute("currentUser", _user);
%>
